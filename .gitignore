# Python
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/

# uv
.uv/

# Environment — never commit secrets
.env
.env.*
!.env.example

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Local data
*.db
*.sqlite3
/data/
/tmp/

# Models (BGE-M3 is downloaded at runtime, not committed)
/models/
.cache/

# Agent working memory — volatile session state, deliberately not committed
# (stable contract lives in AGENTS.md; see templates/agentic-workflow/README.md)
HANDOFF.md

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Design-tool exports keep their own runtime; keep them, but ignore browser cruft
design-reference/**/*.log

# Assembled onboarding bundle for the team — generated from docs/, not source
team-pack/
*.zip
